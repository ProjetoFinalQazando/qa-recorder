name: Schedule - Cypress API Tests
on: 
  schedule:
    - cron: '5 13 5 8 *'  # Executa às 10:05 BRT (13:05 UTC) no dia 05/08
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Clonar o projeto
        uses: actions/checkout@v4
      - name: Cypress run
        uses: cypress-io/github-action@v6
